# Examples

This folder contains generated planning-core examples.

- `planning-input-template.xlsx`: sample input workbook generated by `python -m engine.planning_core.cli create-template --out docs/examples/planning-input-template.xlsx`.

The template contains runnable sample rows. Edit the rows while preserving sheet names and headers.
